<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plain HTTP only to local dev addresses. 10.0.2.2 is the Android
  emulator's special alias for the host machine's localhost (the emulator
  runs in its own network namespace, unlike the iOS Simulator which shares
  the host's directly) — see ApiClient.kt's baseURL comment.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
